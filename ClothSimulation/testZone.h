#pragma once

#include "Core.h"
#include "ParticleSystem.h"

ParticleSystem ps(4, glm::vec3(0.0f));

using namespace std;

// Test particles with only gravity
void test01()
{
	//ParticleSystem ps(3, glm::vec3(0.0f));
	//ps.printSprings();
}